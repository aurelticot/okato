const timelineVisiblePeriod = 24;
const daysInPast = 3;
const daysInFuture = 3;
const defaultLocale = "en";

export default {
  defaultLocale,
  timelineVisiblePeriod,
  daysInPast,
  daysInFuture,
};
